{
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "db:import": "npm run db:import --prefix backend",
    "setup": "npm run install:all && npm run db:import",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start": "concurrently --kill-others-on-fail -n backend,frontend -c green,cyan \"npm run start:backend\" \"npm run start:frontend\""
  }
}
